# 🏗️ AIforXuX 架构设计文档

## 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        AIforXuX 系统                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐              │
│  │ 00_memory │  │  shared   │  │    docs          │              │
│  │ 记忆中枢  │  │ 公共模块  │  │  项目文档        │              │
│  └────┬─────┘  └─────┬────┘  └──────────────────┘              │
│       │              │                                          │
│  ─────┼──────────────┼──────────────── 核心层 ───────────────   │
│       │              │                                          │
│  ┌────┴──────────────┴──────────────────────────────────────┐   │
│  │                    功能模块层                              │   │
│  │                                                          │   │
│  │  01_game_info         │  02_game_interaction_design      │   │
│  │  03_document_processing│  04_calendar_meeting            │   │
│  │  05_knowledge_base    │  06_data_analytics               │   │
│  │  07_general           │  08_tools                        │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            │                                    │
│  ─────────────────────────────────────── 工程项目层 ─────────   │
│                            │                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    project/ 工程项目                       │   │
│  │                                                          │   │
│  │  game_tracker/          │  king_world_tracker/           │   │
│  │  （多平台游戏数据追踪）   │  （王者世界舆情监控）            │   │
│  │                         │                                │   │
│  │  ui_usability_analyzer/ │  desktop_pet/                  │   │
│  │  （界面易用性分析引擎）   │  （桌面 AI 宠物）               │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            │                                    │
│                     数据流向（示例）                              │
│   collectors → project/analyzer → storage/reports → HTML报告    │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │             09_update_log  更新记录层                      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 设计原则

1. **编号模块制**：每个模块以编号开头，便于排序和引用
2. **Soul 驱动**：每个模块的 `soul.md` 是 AI 理解该模块的入口
3. **记忆优先**：`00_memory` 作为第一个模块，是 AI 的核心记忆系统
4. **渐进式扩展**：模块可独立展开，不影响其他模块
5. **工程分离**：`project/` 目录存放可独立运行的工程项目，与功能模块文档层解耦

## 模块间关系

- `00_memory` ↔ 所有模块：记忆系统为所有模块提供上下文
- `shared` → 所有模块：公共配置、工具函数、接口定义
- `09_update_log` ← 所有模块：记录所有模块的变更历史
- `01_game_info/collectors` → `project/` → `storage/reports`：SKILL 文档定义能力，project 目录承载工程实现
- 各功能模块之间相互独立，通过 `shared` 层进行解耦

## 文件约定

| 文件名 | 用途 | 必需 |
|--------|------|------|
| `README.md` | 模块说明文档 | ✅ 是 |
| `soul.md` | AI 工作风格与思维定义 | ✅ 是 |
| `SKILL_*.md` | 具体技能的完整定义文档 | 按需 |
| `entries/` | 数据/记录条目存储目录 | 按需 |

## 工程项目说明

| 项目 | 路径 | 功能 | 状态 |
|------|------|------|------|
| game_tracker | `project/game_tracker/` | 多平台（Steam/Twitch/移动端）游戏数据追踪 + 定时报告 | 🟢 运行中 |
| king_world_tracker | `project/king_world_tracker/` | 王者世界舆情监控（微博/B站/AppStore/新闻）| 🟢 运行中 |
| ui_usability_analyzer | `project/ui_usability_analyzer/` | 游戏截图 UI 易用性分析引擎（7维度 + HTML报告）| 🟢 Phase 1 完成 |
| desktop_pet | `project/desktop_pet/` | 桌面 AI 宠物应用（悬浮窗 + 气泡交互）| 🟢 运行中 |

---

*最后更新: 2026-05-08*